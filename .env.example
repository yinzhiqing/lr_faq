# 复制为 .env 后由 docker compose 读取
# openssl rand -base64 48
SESSION_SECRET=

# 映射到宿主机的端口
PUBLISH_PORT=3000

# 置于反向代理后时设为 1
# TRUST_PROXY=1

# 站点为 HTTPS 且前端经代理访问时设为 true（否则登录态 cookie 可能无法写入）
# SESSION_COOKIE_SECURE=true
